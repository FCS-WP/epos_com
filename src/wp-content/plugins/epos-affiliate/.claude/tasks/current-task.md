# Current Task

No active task. Ready for next assignment.

## Recently Completed
- Admin MUI confirmation dialogs for Commission (approve/paid/void), Reseller & BD (deactivate/reactivate)
- Admin QR code popup dialogs for both Reseller and BD lists
- Custom branded HTML welcome emails (`EmailService`) replacing default WP emails
- Change password feature (`PUT /profile/password`) with full security validation
- Dashboard page access protection — non-logged-in users redirect to `/my/login/`
- Reseller API enriched with `qr_token` and `tracking_code` from auto-created BD record
